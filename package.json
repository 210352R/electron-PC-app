{
  "name": "electron-pc-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "react-build": "cd app && npm run build",  
    "start": "npm run react-build && electron .",  
    "dev": "nodemon --exec electron .  ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "react-start": "cd app && npm start" 
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^32.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
